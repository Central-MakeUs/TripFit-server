# ============================================================
# EC2 B — MySQL 설정
# ============================================================

MYSQL_DATABASE=tripfit
MYSQL_ROOT_PASSWORD=
MYSQL_PORT=3306
MYSQL_BIND=0.0.0.0

# EC2 C(모니터링) private IP — docs/decisions/009-observability-logging.md. EC2 B는 CI/CD 대상이 아니므로 이 값을 직접 유지
LOKI_HOST=<EC2 C private IP>
