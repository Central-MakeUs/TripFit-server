# ============================================================
# EC2 D — Redis 설정 (docs/decisions/010-redis-infra.md)
# ============================================================

REDIS_PASSWORD=
REDIS_PORT=6379
REDIS_BIND=0.0.0.0

# EC2 C(모니터링) private IP — docs/decisions/009-observability-logging.md. EC2 D는 CI/CD 대상이 아니므로 이 값을 직접 유지
LOKI_HOST=<EC2 C private IP>
