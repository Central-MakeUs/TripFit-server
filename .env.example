# MySQL (docker compose up -d)
MYSQL_ROOT_PASSWORD=tripfit
MYSQL_DATABASE=tripfit
MYSQL_PORT=3306

# Spring Boot — bootRun(local)이 Docker MySQL에 접속
MYSQL_HOST=localhost
SPRING_DATASOURCE_USERNAME=root
SPRING_DATASOURCE_PASSWORD=tripfit

APP_PORT=8080
APP_PUBLISH_HOST=127.0.0.1

# Nginx API 게이트웨이 (docker compose --profile edge --profile app)
NGINX_HTTP_PORT=80
NGINX_HTTPS_PORT=443
CERTBOT_DOMAIN=api.tripfit.online

# TripFit JWT
JWT_SECRET=local-dev-secret-must-be-at-least-32-chars
JWT_ACCESS_EXPIRATION=7200
JWT_REFRESH_EXPIRATION_DAYS=30

# Google 로그인 (auth 도메인 — audience 검증 + authorization code 교환)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_CLIENT_ID_IOS=
GOOGLE_CLIENT_ID_ANDROID=

# Google Calendar 연동 전용 (로그인과 분리된 별도 Client ID — #78)
GOOGLE_CALENDAR_CLIENT_ID=
GOOGLE_CALENDAR_CLIENT_SECRET=

# Apple
APPLE_BUNDLE_ID=
APPLE_SERVICE_ID=
APPLE_TEAM_ID=
APPLE_KEY_ID=
APPLE_PRIVATE_KEY=

# Kakao
KAKAO_ADMIN_KEY=

# 소셜 refresh token(Apple 로그인·Google 로그인·Google Calendar) 암호화 공용 AES-256-GCM 키
SOCIAL_TOKEN_AES_KEY=

# FCM (docs/specs/notification.md — D4 FCM 단일 채널). 서비스 계정 JSON을 base64 인코딩해 통째로 넣는다.
# 로컬 생성: base64 -i config/{project}-firebase-adminsdk-*.json | tr -d '\n'
FIREBASE_CREDENTIALS_BASE64=
